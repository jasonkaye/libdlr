# Compile the id_dist library, and all other core programs and utilities

cd id_dist
make
cd ..

cp id_dist/id_lib.a lib-static
gfortran -c src/utils_external/*.f
gfortran -c src/utils_external/*.f90
gfortran -c src/*.f90
mv *.o bin 
